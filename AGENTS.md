# 地方議員マップ（chiho-map）データ品質ガイドライン

このプロジェクトの**絶対ルール**。違反した場合は本番デプロイ前に必ず修正する。

過去に diet-map 側で「自民党議員が岸田内閣不信任案に賛成」という重大バグが外部ユーザに指摘されて発覚した（議事録パーサの過剰キャプチャ + status 固定が原因）。地方議員マップでも同種事故を防ぐため、以下のルールを徹底する。

---

## 1. データの分離ルール（最重要）

### 1-A. 「発言」と「実績」を絶対に混ぜない

| データ種別 | 入れて良いもの | 禁止 |
|---|---|---|
| `notableQuotes`（本人発言・議会質問） | 議会質問・本会議発言・本人公式SNS投稿・本人公式ブログの**直接引用のみ** | 実績の要約、第三者解説、政治的背景説明、評価語 |
| `careerMilestones`（経歴・役職） | 公式資料記載の事実（当選回数・役職・在任期間・所属委員会・学歴・職歴・議長就任等） | 評価的記述、地盤継承、入党経緯、政策テーマ解釈 |
| `officialSources`（出典リンク） | URL + ラベルのみ（議会公式/政党公式/選管/本人公式/自治体公式） | 自由記述、報道・第三者記事 |
| `overrides.reason`（内部メモ） | データ修正の根拠メモのみ。フロント表示しない | 未確認情報、推測、評価語 |

### 1-B. 出典は必須

- `notableQuotes` / `careerMilestones` / `officialSources` の全項目に `sourceUrl` + `verifiedAt`（確認日）必須
- 出典URLがない情報は**本番表示しない**
- 一次情報のみ（議会公式・自治体公式・本人公式・政党公式・選管・官公庁）。報道・第三者記事は不採用

---

## 2. 禁止語・評価表現

以下の語は本文・reason・biography で**一切使わない**（事実を出典付きで並べ、判断は利用者に委ねる）。

### 推測表現
「らしい」「と思われる」「未確認」「噂」「推測」「だろう」「可能性」「思われ」

### 政治的レッテル
「親中」「極右」「極左」「反日」「ネトウヨ」「パヨク」「反社」

### 評価語・印象操作
「疑惑」「問題」「利権」「金権」「黒幕」「腹黒」「企んでいる」「裏で」「失脚」「降ろされた」「追放」

### ストーリー化・血縁強調
「地盤継承」「政治家家系」「遺志を継いで」「○○一族」「○○系」「権力争い」

### 過度な評価
「貢献した」「実現した」「成功させた」「実績を作った」「英断」「腕利き」

→ 代わりに「○○を本会議で提案したと記載されている」「○○の事務局長を務めた」のように、事実 + 出典 + 引用形式で記述する。

`overrides.reason` / `biography` / `notableQuotes.content` / `careerMilestones.content` のすべてが対象。`scripts/audit/` がスキャンで自動検出（error 化）。

---

## 3. センシティブ情報の禁止

- 帰化歴、出自、宗教、家族の国籍は**一切掲載しない**
- 噂レベルの所属団体、人格攻撃的な情報も禁止
- 扱うのは：発言、投票行動、政策、議会活動、役職、委員会、政治資金、公的資料に基づく情報のみ

---

## 4. 推測で埋めない

- 分からない項目は「不明」「未確認」「現在収集中」で止める
- AI で推測・補完しない
- 日付が特定できない発言・実績は `careerMilestones` の活動として登録するか、登録自体を見送る

---

## 5. 必須監査コマンド

データ変更後は**必ず**以下を実行し、error 0 を確認してからデプロイ：

```bash
NPM_CONFIG_UPDATE_NOTIFIER=false npx --yes tsx scripts/audit/index.ts
NPM_CONFIG_UPDATE_NOTIFIER=false npx --yes tsc --noEmit
```

audit に error が 1件でも出たらデプロイしない。

---

## 6. iCloud 同期下の制約

`/Users/keiai/Desktop/` 配下は iCloud 同期されている。以下を**ローカルで実行しない**：

- `next dev` / `next start` / `preview_start`（プロセスが固まる）
- `next build` の長時間実行

代わりに：
- 軽量検証は `npx esbuild --loader=tsx <file>` で構文チェック
- 監査は `npx --yes tsx scripts/audit/index.ts` で OK（一回完結なら安全）
- **実ビルド・動作確認は Vercel デプロイ後に curl / WebFetch で行う**

---

## 7. やってはいけないこと

- ❌ 議員データを変更してから監査未実行のままデプロイする
- ❌ 「目視確認しました」だけの自然言語レポートで作業完了とする
- ❌ 異常を検出しても「軽微なので無視」と判断する（必ず中島さんに報告し判断を仰ぐ）
- ❌ 監査で warning が出たら無視せず原因を記録する
- ❌ 「発言」セクションに役職・実績を入れる
- ❌ 出典URLなしの情報を本番表示する
- ❌ 評価語・センシティブ情報を含める

---

## 8. Claude が自分で判断して進める

- 「進めていいか？」と1件ずつ聞いて作業を止めるのは禁止
- 安全側に倒して実装を前進させる
- まとまった単位（5名/10名/1自治体/1機能）で完了報告を出す
- 報告形式: 【実装範囲】【追加情報】【出典】【未確認】【リスク】【チェック】【公開判断】【監査回す点】

例外（中島さんに事前相談すべき）：センシティブ情報、法的リスク、お金や投稿仕様、大規模設計、公開前最終監査、X 告知前。
